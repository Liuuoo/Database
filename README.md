# Database
一个轻量数据存储引擎，支持简单的增删查改，用作 C++ 的练手项目。

## 项目进度
以下内容同步展示自 [docs/进度表.md](docs/进度表.md)，方便在 GitHub 首页直接查看当前进展。

### 前期
在前期阶段，学习了存储引擎常用的存储结构，以及利用该结构特性而做出的一些相应的架构设计。了解LSM完整的架构，数据流向。

### 3/29
1. 创建了本地仓库并连接好了github。
2. 完成了整体骨架的创建

### 3/30
1. 下载CMake，了解CMake工作原理，更新gcc环境，从原来的8.1.x更新到了15.1.0。遇到了安装确实.dll库的问题，下载别的版本解决。注意删除带有环境变量的文件，在终端执行命令非常有效，粗暴`rd /s /q "E:\MinG-W64\mingw64"`
2. 学习并配置了CMake，使用Build构建了项目可执行文件，成功输出hello world。
3. 借助工具完成计划表的编写，主要是制定了阶段1的任务。

### 4/1
1. 学习了解跳表，制定跳表实现细节。比如随机数如何制作，最大层数限制。
2. 了解内存中大致的架构，主要是Arena分配机制与Immutable MemTable机制。
3. 了解磁盘中大致的架构，主要是WAL、SSTable问题处理、BloomFliter检查机制、合并机制

### 4/2
1. 对阶段1的设计内容做了收尾整理，补充了阶段2相关的文档资源与示意图，并继续明确后续实现顺序。
2. 开始从文档阶段转入代码骨架阶段，建立了 interface、engine、memtable 相关目录与文件，补出了 command、parser、record 等接口层初稿，同时把 main 调整为读取输入的交互入口雏形。
3. 整理了测试目录结构，将测试文件统一放到 tests 目录下，并把当天整理后的多次提交推送到了 github，便于后续按阶段继续开发。

### 4/3
1. 将进度表同步到 README 首页，方便在 GitHub 仓库首页直接查看项目推进情况。
2. 保持 README 与 docs/进度表.md 的进度记录一致，后续继续按日期补充开发日志。

### 4/4
1. 完成了部分对外接口的开发。
2. 观看了B站前百度员工对LevelDB的讲解，叹为观止望洋兴叹，才知道LevelDB的强大以及了解了Jeff Dean的牛逼。

### 4/5
基本完成了对外接口的开发，能正确地读取cli中的命令，能进行简单的异常抛出等。下一阶段计划开始内存结构memtable的开发。
